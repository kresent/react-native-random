import numbers from './numbers';

describe('numbers model', () => {
  it('should generate exactly three values', () => {
    // TODO: write unit test
    expect().toEqual({});
  });

  it('should generate unique values', () => {
    // TODO: write unit test
    expect().toEqual({});
  });
});
